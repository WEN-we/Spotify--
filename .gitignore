# ── 依赖 ──
node_modules/

# ── 构建产物（源码可重建） ──
windows/dist/
windows/build/
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/core/build/
android/local.properties

# ── 运行日志 ──
*.log
*.access.log

# ── 运行时数据（各机器本地生成） ──
windows/services/qq-proxy-cache.json

# ── IDE / 系统文件 ──
.idea/
*.iml
.DS_Store
Thumbs.db
