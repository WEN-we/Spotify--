# 2026-09-08 Spotify工具 工作日志

## 接手 TRAE 未完成任务：QQ音乐搜索抗封锁（v1.2.0）收尾

### 背景
TRAE 已写完主体代码但未验证/未提交：Android 端 QQ 音乐搜索因 2026-09 QQ 封锁
client_search_cp/musicu.fcg 而做的四链路降级改造（代理→网页→桌面 musicu→smartbox）+
歌手感知匹配（防「Give up→江声入旧年」类错配）+ 局域网代理复用电脑缓存（5 分钟熔断）。
Windows 端同步改了 ProviderQQMusic.js（smartbox 回退）、qqProxy.mjs（/sb 端点+0.0.0.0 监听）、
seed-cache-from-log.mjs（严格相邻配对+[ti:] 交叉验证）。

### 本次完成
1. Windows `npm test` 16/16 通过；Android `:app:testDebugUnitTest` 24/24 通过（QqMusicClientTest 新增）
2. `assembleRelease` 构建成功 → android/app/build/outputs/apk/release/app-release.apk（v1.2.0, 689KB）
3. 补文档：根 README 修正 qqProxy 监听说明（回环→0.0.0.0）+ Android 亮点补「搜索抗封锁」条目
4. 提交 7fd5e59（14 文件 +810/-77）并推送 GitHub（WEN-we/Spotify--）成功
5. 创建附注 tag v1.2.0（本地完成）

### 遗留
- **tag v1.2.0 推送被网络卡住**：本机代理 127.0.0.1:56063 返回 502（VPN/GFW 老问题）。
  网络恢复后执行 `git push origin v1.2.0` 即可 ✅（11:06 已推送成功）
- **PortableGit 1.2.0 (WorkBuddy 自带) 在非 ASCII 路径下对 refs/remotes/* 的写入静默失败**
  （update-ref exit 0 但 ref 文件不落地；reflog 正常；refs/heads 和 refs/tags 不受影响）。
  已手动写 SHA 到 .git/refs/remotes/origin/master 修复跟踪。以后 git status 再现 [gone] 同样手动补即可
- GitHub Releases 未建（gh 未安装，用户习惯网页端管理）

## 下午追加：用户反馈两问题 → v1.2.2（未发布，待真机验证）

### 问题1 Windows 部分歌曲无歌词
- 根因A：Windows 端缺 musicu.fcg 链路 → qqProxy /search、/s 内部加 musicu 降级
  （客户端响应格式不变，日志 via=musicu 标记），实测封锁期返回 10 候选 ✅
- 根因B：「GIve up」QQ 无原唱（只有 Seven. 等翻唱），歌手感知匹配正确拒绝 → 用户要求兜底

### 问题2 Android 不同步
- 加「同步诊断」卡片：通知监听连接/Spotify 会话绑定/最近播放事件三层状态 + requestRebind 重连按钮

### 匹配兜底（用户明确要求）
- 规则5：歌名相等（忽略大小写/繁简）取搜索首位（热度序）；规则6：歌名包含取首位
- 原唱优先不变；纯时长匹配仍禁止（跨歌曲错配依旧不可能）
- 坑：matchCandidate 里调 LogKit.d → AppConfig.prefs 未初始化崩溃单测；纯函数原则，日志移出

### 构建环境重大发现（重要！）
- Gradle 8.13 测试 worker classpath 经 java @argfile 传递：daemon 按 file.encoding=UTF-8 写入、
  JDK 启动器按系统 GBK 读 → 中文路径乱码 → 所有模块测试 ClassNotFoundException（最小复现已验证）
- 修复：android/gradle.properties jvmargs file.encoding=GBK（两端口径一致）
- 沙箱会拦截 Gradle 对 app/build/intermediates 的读写 → 构建需 dangerouslyDisableSandbox

### 状态
- v1.2.2（versionCode 6）26/26 测试通过，已 adb 安装到真机 24069RA21C（小米 peridot，adb 在
  C:/Program Files (x86)/Android/android-sdk/platform-tools/）
- Windows 端 Provider 已 deploy + spicetify apply 生效（注意：沙箱 shell 无 APPDATA 环境变量，
  裸跑 spicetify 会重置配置，必须 APPDATA=... 显式传入）
- **本地提交 e6b66a1 未推送未打 tag**，等用户手机播 Give up 验证通过后再发布

## 真机根因实锤：MIUI 自启动拦截（11:26-11:35）

### 用户反馈
- Windows 端 Give up 兜底 ✅ 有效
- APP 卡「等待播放」❌

### adb 取证（被动，logcat + dumpsys）
- `AutoStartManagerService: MIUILOG- Reject service ...PlaybackListenerService` +
  `NotificationListeners: AutStart Unable to bind notification listener service`
- 系统多次尝试重绑（requestRebind 生效了）全被 MIUI 自启动管理拒绝 → 监听永不连接 → 永远等待播放
- 权限（enabled_notification_listeners）本身在，Manifest 正确；纯粹是 MIUI 自启动白名单问题
- MIUI 自启动不走 appops（AUTO_START unknown op），只能用户在安全中心手动开
- 附加：用户测试时手机 Spotify 根本没在运行（pidof 空）——注意 App 只同步手机 Spotify，不同步电脑播放

### 修复（v1.2.2 versionCode 7 已重装手机）
- 诊断卡片新增「去开自启动」按钮：直接跳 com.miui.securitycenter AutoStartManagementActivity
  （非 MIUI 回退应用详情页），指引文案同步更新
- 用户操作序列：去开自启动允许 → 重新连接 → 手机 Spotify 播歌

## 自启动开启后仍失败的排障（11:38-11:42）→ 已解决
- 用户开自启动后仍「等待播放」；取证：Spotify 在运行且有 MediaSession，但监听仍未连
- `cmd notification allow_listener <component>` adb 重发授权 → 触发系统重绑成功（自启动已放行，不再 Reject）
- **11:42:17 全链路打通**：通知监听已连接 → 已绑定 MediaSession → 2s 轮询发布《江聲入舊年》
- 经验：MIUI 上自启动放行后，还需要一次 rebind 触发；allow_listener / 手动重开通知使用权 / 重启手机均可
- 新发现数据问题：《楓》在手机端 smartbox 匹配失败（PC 端 curl 验证 smartbox 返回 枫/718479/周杰伦
  本应命中）→ v8 (versionCode 8) 在 matchCandidate 失败路径加了候选明细日志（debugLog 可见），
  待复现后拉 logcat 定位
- 用户看到的「等待播放」是监听连接前状态；连上后 UI 会自动刷新，无需重开 App

## 看门狗自愈 + 效率说明（12:45-12:55, v9 未发布）

### 用户反馈
- App 重启几次后又「等待播放」；问歌词加载能否提速

### 取证结论
- v8 其实一直在正常工作（12:48 前持续发布《花雨弄》播放状态），「复发」模式 =
  进程被杀（用户划掉/MIUI 清理）→ MIUI 不自动重绑 → 直到外部触发（allow_listener/开权限）
- 双源竞速确认已是真并行（newFixedThreadPool(2)），效率瓶颈不在代码结构

### v9 (versionCode 9) 修复
- **LyricsService 重绑看门狗**：每 10s 检查 listenerConnected，掉线且有权限 → requestRebind（节流 15s）
  → App 重启/进程被杀后打开即自愈，无需手动重连
- QQ 直连 connectTimeout 8s→5s（封锁期快速切链）
- 26/26 测试通过，已装手机；本地提交 beffe2d 未推送
- 提速建议（未改代码的部分）：手机「设置→歌词代理」填 PC 地址 → 命中 PC 持久缓存近乎零延迟；
  LRCLIB 海外服务器在国内网络偏慢，QQ 源正常时竞速会胜出

## MIUI 最终结论（12:51-12:55, commit 4518235）
- **实锤：自启动授权后，系统自动重绑/requestRebind/看门狗 重绑仍被 AutoStartManagerService 拦截**
- 设备端唯一稳定有效：用户手动「通知使用权 关→开」（用户主动触发的绑定不拦）
- adb `cmd notification allow_listener` 每次有效（等价系统内部重注册，需连 PC）
- 当前手机已连上（12:55:32 发布《花雨弄》+缓存命中秒出歌词）——用户提问的「先开 Spotify 后开 App」场景实测支持
- reconnectListener 增强：3s 未连自动跳通知使用权设置页；诊断卡/README 恢复顺序已更新
- 发版待用户验证 Give up 兜底后进行
