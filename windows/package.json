{
  "name": "spotify-t2s-converter",
  "version": "1.0.0",
  "description": "Spotify 界面繁体转简体 Spicetify 扩展",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "build": "node build/build.js",
    "deploy": "node build/build.js --deploy",
    "test": "node --test tests/*.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "opencc-js": "^1.0.5"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^24.0.0"
  }
}
